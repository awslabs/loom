# Copy to .env at the repo root. .env is gitignored.
# LOCAL DEVELOPMENT ONLY.

# LiteLLM proxy master key — must match docker-compose LITELLM_MASTER_KEY / LOOM_LITELLM_PROXY_API_KEY.
LITELLM_MASTER_KEY=sk-loom-local-dev

# Cursor adapter (compose service cursor-adapter). Never set these on the Loom backend.
# Get a user key at https://cursor.com/dashboard/integrations
# Without CURSOR_API_KEY the adapter still starts; completions return 401.
CURSOR_API_KEY=
# Host path mounted into the adapter as /workspace. Empty = this repository.
# Docker Engine is in WSL: use a Linux path, not C:\...
# Example: /mnt/c/work/Personal/repos/demo-agent
CURSOR_WORKSPACE=
CURSOR_MODEL=composer-2.5

# Shared token between the backend and the mcp-runtime compose service.
# LOCAL DEVELOPMENT ONLY — never reuse in a deployed environment.
MCP_RUNTIME_TOKEN=loom-mcp-runtime-local

# Shared token between the backend and the agent-runtime compose service.
AGENT_RUNTIME_TOKEN=loom-agent-runtime-local

# Optional. Injected into the Azure DevOps MCP child as ADO_MCP_AUTH_TOKEN.
AZURE_DEVOPS_PAT=

# Optional. Grafana MCP (stdio template `grafana`) — service account token.
# Loom UI param grafana_url: http://grafana.local:8080
GRAFANA_SERVICE_ACCOUNT_TOKEN=

# Optional. Rancher MCP (stdio template `rancher`) — API token (token-xxx:yyy).
# Loom UI param rancher_server_url (mcp-runtime on Docker kind net):
#   http://kind-control-plane:30080
# Stdio on WSL/Windows host (not this container): http://rancher.local:8080
RANCHER_MCP_RANCHER_TOKEN=
# Usually unset for HTTP :30080 / :8080. Set true only for HTTPS self-signed.
RANCHER_MCP_TLS_INSECURE=
